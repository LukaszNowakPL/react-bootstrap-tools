export interface DescribedButtonProps {
    description?: string;
    buttonType?: string;
    buttonName?: string;
}

export interface ButtonProps {
    buttonType?:string,
    buttonName?:string,
}

export interface Aaa {
    description?: string,
}