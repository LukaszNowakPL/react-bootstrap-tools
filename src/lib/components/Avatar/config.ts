import { ConfigInterface } from "./interfaces";

const config: ConfigInterface = {
  defaultIcon: "person",
  defaultWrapperClassName: "Avatar",
  defaultImageClassName: "Avatar__image",
  defaultIconClassName: "Avatar__icon"
};

export default config;
